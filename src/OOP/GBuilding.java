package OOP;

public class GBuilding extends Building {
    GBuilding(int x, int y, int w, int h) {
        super(x, y, w, h);
    }

    void drawBuilding() {
        // g2d.drawRect(x, y, w, h);
    }
}